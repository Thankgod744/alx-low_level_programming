#include "main.h"

/**
 * print_number - func checks for checks for a digit (0 through 9)
 *
 * return: Always 0
 */
void print_numbers(void)
{
	int c;

	for (c = 40; c < 58; c++)
	{
		_putchar(c);
	}
		_putchar('\n');
}
